# Obscura v1 does not require runtime secrets or API keys.
#
# Keep this file as the public environment contract. Add names only, never
# private values, if future build-time flags or asset paths are introduced.

